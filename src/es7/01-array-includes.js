//Con js no es necesario recorrer el array
//Con el includes se puede hacer
let numeros = [1, 2, 3, 4, 5, 6, ];

console.log(numeros.includes(4));

//Ojo con los valores, si lo dejo así no lee a Exequiel,
//Porque esta con letra minúscula
const list = ['Exequiel', 'Soledad', 'Tiziana'];
console.log(list.includes('Exequiel'));